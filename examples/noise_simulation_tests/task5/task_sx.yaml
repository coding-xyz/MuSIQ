schema_version: "3.0"
target: trajectory
input:
  qasm_text: |
    OPENQASM 3;
    include "stdgates.inc";
    qubit[1] q;
    sx q[0];
  solver_config: solver.yaml
  device_config: device.yaml
  pulse_config: pulse_drag.yaml
  analyser_config: analyser.yaml
output:
  out_dir: ../runs/task5_h
  persist_artifacts: true
  artifact_mode: targeted
  export_plots: false
  export_dxf: false
features: {}
tags: [task5, single-qubit, sx-gate]
