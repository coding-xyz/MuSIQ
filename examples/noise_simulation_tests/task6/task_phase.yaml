schema_version: "3.0"
target: trajectory
input:
  qasm_text: |
    OPENQASM 3;
    include "stdgates.inc";
    qubit[2] q;
    ry(1.5707963267948966) q[0];
    ry(1.5707963267948966) q[1];
    cz q[0], q[1];
  solver_config: solver.yaml
  device_config: device.yaml
  pulse_config: pulse.yaml
  analyser_config: analyser.yaml
output:
  out_dir: ../runs/task6_two_qubit_cz_phase
  persist_artifacts: true
  artifact_mode: targeted
  export_plots: false
  export_dxf: false
features: {}
tags: [task6, two-qubit, cz-gate, phase-demo]
