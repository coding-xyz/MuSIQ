schema_version: "3.0"
target: trajectory
input:
  qasm_text: |
    OPENQASM 3;
    include "stdgates.inc";
    qubit[1] q;
    bit[1] c;
    x q[0];
    measure q[0] -> c[0];
  solver_config: qutip.yaml
  device_config: device.yaml
  pulse_config: pulse.yaml
output:
  out_dir: ./runs
  artifact_mode: targeted
  persist_artifacts: true
  export_plots: false
  export_dxf: false
features: {}
tags: [task1]

